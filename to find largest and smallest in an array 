#include<stdio.h>
int main()
{
    int a[100],i,largest,smallest;
    printf("\n Enter the array elements ");
    for(i=0;i<10;i++)
    {
    scanf("%d",&a[i]);
    }
    for(i=0;i<10;i++)
    {
        printf("\t %d",a[i]);
    }
    largest=a[0];
    for(i=0;i<10;i++)
    {
        if(a[i]>largest)
        {
            largest=a[i];
        }
    }
    {
    printf(" \nthe largest elements are:%d\n",largest);
    }
    smallest=a[0] ;
    for(i=0;i<10;i++)
    {
        if(a[i]<smallest)
        {
            smallest=a[i];
        }
    }
    printf("\n the smallest elements are:%d",smallest);
    
}
    
